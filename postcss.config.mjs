// postcss.config.mjs
const config = {
  plugins: {
    tailwindcss: {}, // Eller 'tailwindcss: {}' hvis det er en .js fil
    autoprefixer: {}, // Eller 'autoprefixer: {}'
  },
};

export default config;
